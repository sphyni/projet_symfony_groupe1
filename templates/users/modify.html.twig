{% extends 'layout.html.twig' %}

<tr>
    <td>
        {{ form_start (profilEditType) }}
        {{ form_widget(profilEditType) }}
    </td>
    <td><button type="submit">Ajouter</button> </td>
    {{ form_end(profilEditType) }}
</tr>

<div class="button">
    <button type="button" id="btnModifier" class="button btn-sm" href="#"  >Enregistrer</button>
    <button type="button" id="btnRetour" class="button btn-sm" href="#">Annuler</button>
</div>
