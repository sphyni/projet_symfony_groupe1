{% extends 'layout.html.twig' %}
{% block main %}
    <div class="form_sortie">
        {{ form_start(lieuForm) }}
        {{ form_widget(lieuForm) }}
        <button><a href="{{ path('villes') }}">Céer une ville</a></button>
        <button><a href="{{ path('sorties') }}">Annuler</a></button>
        {{ form_end(lieuForm) }}
    </div>
{% endblock %}