Formulaire de la page create-sortie.html.twig
{#
{{ form_start(siteForm) }}
{{ form_widget(siteForm) }}
{{ form_end(siteForm) }}


{{ form_start(villeForm) }}
{{ form_widget(villeForm) }}
{{ form_end(villeForm) }}


{{ form_start(lieuForm) }}
{{ form_widget(lieuForm) }}
{{ form_end(lieuForm) }}
#}



    {{ sortie.getId() }}
    {{ sortie.getNom() }}
    {{ sortie.getDateHeureDebut()|date('d-m-y à h:i') }}
    {{ sortie.getDuree()|date('d-m-y à h:i') }}
    {{ sortie.getDateLimiteInscription()|date('d-m-y à h:i') }}
    {{ sortie.getNbInscriptionsMax() }}
    {{ sortie.getInfosSortie() }}
    {{ sortie.getHistorique() }}


    {{ sortie.etat().getId() }}
    {{ sortie.etat().getLibelle() }}

    {{ sortie.site().getId() }}
    {{ sortie.site().getNom() }}

    {{ sortie.lieu().getId() }}
    {{ sortie.lieu().getNom() }}
    {{ sortie.lieu().getRue() }}
    {{ sortie.lieu().getLatitude() }}
    {{ sortie.lieu().getLongitude() }}

<form>
    <select >
        {{ for site nom }}{
        <option value="nom"></option>
    </select>
    <input type="submit">
</form>

<table>
    <thead>
    <tr>
        <th>Nom de la sortie</th>
        <th>Date de la sortie</th>
        <th>Clôturé</th>
        <th>Inscription/Places</th>
        <th>Etat</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    <td>{{ sortie.getNom() }}</td>
    <td>{{ sortie.getDateHeureDebut() }}</td>
    <td>{{ sortie.getDateLimiteInscription() }}</td>
    <td>{{ sortie.getNbInscriptionMax() }}</td>
    <td>{{ sortie.Etat().getEtat() }}</td>
    <td><a href="#">Afficher</a></td>
    </tbody>
</table>


{{ form_start(etatForm) }}
        {{ form_widget(etatForm) }}
        {{ form_end(etatForm) }}